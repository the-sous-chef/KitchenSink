# Acceptance Test Plan: Cooking School (Video Learning Platform)

**Feature Branch**: `013-cooking-school`
**Created**: 2026-05-10
**Status**: Draft
**Source**: `specs/013-cooking-school/v-model/requirements.md`

## Overview

This acceptance plan maps every requirement (`REQ-001..REQ-028`) to at least one executable ATP/SCN pair.

## Acceptance Tests

### Requirement Validation: REQ-001 (allow educators to create courses with title, description, thumbnail, an)

#### Test Case: ATP-001-A (REQ-001 validation)

**Description:** Validates REQ-001 via **Test** against the requirement contract.

- **User Scenario: SCN-001-A1**
    - **Given** prerequisite identity, policy, and data setup required for the requirement
    - **When** the actor executes the feature behavior under normal v1 conditions
    - **Then** the expected requirement outcome is observed with no policy or traceability violation

---

### Requirement Validation: REQ-002 (allow educators to add lessons to a course with title, description, tran)

#### Test Case: ATP-002-A (REQ-002 validation)

**Description:** Validates REQ-002 via **Test** against the requirement contract.

- **User Scenario: SCN-002-A1**
    - **Given** prerequisite identity, policy, and data setup required for the requirement
    - **When** the actor executes the feature behavior under normal v1 conditions
    - **Then** the expected requirement outcome is observed with no policy or traceability violation

---

### Requirement Validation: REQ-003 (accept video uploads and transcode each lesson to HLS renditions (720p a)

#### Test Case: ATP-003-A (REQ-003 validation)

**Description:** Validates REQ-003 via **Test** against the requirement contract.

- **User Scenario: SCN-003-A1**
    - **Given** prerequisite identity, policy, and data setup required for the requirement
    - **When** the actor executes the feature behavior under normal v1 conditions
    - **Then** the expected requirement outcome is observed with no policy or traceability violation

---

### Requirement Validation: REQ-004 (deliver lesson playback via CDN with signed playback URLs for enrolled l)

#### Test Case: ATP-004-A (REQ-004 validation)

**Description:** Validates REQ-004 via **Test** against the requirement contract.

- **User Scenario: SCN-004-A1**
    - **Given** prerequisite identity, policy, and data setup required for the requirement
    - **When** the actor executes the feature behavior under normal v1 conditions
    - **Then** the expected requirement outcome is observed with no policy or traceability violation

---

### Requirement Validation: REQ-005 (expose unsigned preview playback only for designated preview lessons)

#### Test Case: ATP-005-A (REQ-005 validation)

**Description:** Validates REQ-005 via **Test** against the requirement contract.

- **User Scenario: SCN-005-A1**
    - **Given** prerequisite identity, policy, and data setup required for the requirement
    - **When** the actor executes the feature behavior under normal v1 conditions
    - **Then** the expected requirement outcome is observed with no policy or traceability violation

---

### Requirement Validation: REQ-006 (provide a learner catalog listing with educator handle, lesson count, an)

#### Test Case: ATP-006-A (REQ-006 validation)

**Description:** Validates REQ-006 via **Demonstration** against the requirement contract.

- **User Scenario: SCN-006-A1**
    - **Given** prerequisite identity, policy, and data setup required for the requirement
    - **When** the actor executes the feature behavior under normal v1 conditions
    - **Then** the expected requirement outcome is observed with no policy or traceability violation

---

### Requirement Validation: REQ-007 (process one-time course purchases and grant enrollment immediately after)

#### Test Case: ATP-007-A (REQ-007 validation)

**Description:** Validates REQ-007 via **Test** against the requirement contract.

- **User Scenario: SCN-007-A1**
    - **Given** prerequisite identity, policy, and data setup required for the requirement
    - **When** the actor executes the feature behavior under normal v1 conditions
    - **Then** the expected requirement outcome is observed with no policy or traceability violation

---

### Requirement Validation: REQ-008 (enforce published-lesson access so only enrolled authenticated learners )

#### Test Case: ATP-008-A (REQ-008 validation)

**Description:** Validates REQ-008 via **Test** against the requirement contract.

- **User Scenario: SCN-008-A1**
    - **Given** prerequisite identity, policy, and data setup required for the requirement
    - **When** the actor executes the feature behavior under normal v1 conditions
    - **Then** the expected requirement outcome is observed with no policy or traceability violation

---

### Requirement Validation: REQ-009 (track lesson progress using watch_percent, completed_at, and last_watche)

#### Test Case: ATP-009-A (REQ-009 validation)

**Description:** Validates REQ-009 via **Test** against the requirement contract.

- **User Scenario: SCN-009-A1**
    - **Given** prerequisite identity, policy, and data setup required for the requirement
    - **When** the actor executes the feature behavior under normal v1 conditions
    - **Then** the expected requirement outcome is observed with no policy or traceability violation

---

### Requirement Validation: REQ-010 (mark lessons complete automatically when watch_percent reaches or exceed)

#### Test Case: ATP-010-A (REQ-010 validation)

**Description:** Validates REQ-010 via **Test** against the requirement contract.

- **User Scenario: SCN-010-A1**
    - **Given** prerequisite identity, policy, and data setup required for the requirement
    - **When** the actor executes the feature behavior under normal v1 conditions
    - **Then** the expected requirement outcome is observed with no policy or traceability violation

---

### Requirement Validation: REQ-011 (provide a learner progress view across all enrolled courses)

#### Test Case: ATP-011-A (REQ-011 validation)

**Description:** Validates REQ-011 via **Demonstration** against the requirement contract.

- **User Scenario: SCN-011-A1**
    - **Given** prerequisite identity, policy, and data setup required for the requirement
    - **When** the actor executes the feature behavior under normal v1 conditions
    - **Then** the expected requirement outcome is observed with no policy or traceability violation

---

### Requirement Validation: REQ-012 (provide educator analytics with enrollments, lesson completion rates, an)

#### Test Case: ATP-012-A (REQ-012 validation)

**Description:** Validates REQ-012 via **Demonstration** against the requirement contract.

- **User Scenario: SCN-012-A1**
    - **Given** prerequisite identity, policy, and data setup required for the requirement
    - **When** the actor executes the feature behavior under normal v1 conditions
    - **Then** the expected requirement outcome is observed with no policy or traceability violation

---

### Requirement Validation: REQ-013 (support optional recipe linkage on lessons and render linked recipe data)

#### Test Case: ATP-013-A (REQ-013 validation)

**Description:** Validates REQ-013 via **Test** against the requirement contract.

- **User Scenario: SCN-013-A1**
    - **Given** prerequisite identity, policy, and data setup required for the requirement
    - **When** the actor executes the feature behavior under normal v1 conditions
    - **Then** the expected requirement outcome is observed with no policy or traceability violation

---

### Requirement Validation: REQ-014 (request AI script drafts from the AI service for linked recipes using ed)

#### Test Case: ATP-014-A (REQ-014 validation)

**Description:** Validates REQ-014 via **Test** against the requirement contract.

- **User Scenario: SCN-014-A1**
    - **Given** prerequisite identity, policy, and data setup required for the requirement
    - **When** the actor executes the feature behavior under normal v1 conditions
    - **Then** the expected requirement outcome is observed with no policy or traceability violation

---

### Requirement Validation: REQ-015 (allow educators to publish or unpublish lessons independently of course )

#### Test Case: ATP-015-A (REQ-015 validation)

**Description:** Validates REQ-015 via **Test** against the requirement contract.

- **User Scenario: SCN-015-A1**
    - **Given** prerequisite identity, policy, and data setup required for the requirement
    - **When** the actor executes the feature behavior under normal v1 conditions
    - **Then** the expected requirement outcome is observed with no policy or traceability violation

---

### Requirement Validation: REQ-016 (persist educator-defined lesson reorder operations)

#### Test Case: ATP-016-A (REQ-016 validation)

**Description:** Validates REQ-016 via **Test** against the requirement contract.

- **User Scenario: SCN-016-A1**
    - **Given** prerequisite identity, policy, and data setup required for the requirement
    - **When** the actor executes the feature behavior under normal v1 conditions
    - **Then** the expected requirement outcome is observed with no policy or traceability violation

---

### Requirement Validation: REQ-017 (use CreatorProfile from feature 012 as the educator identity source with)

#### Test Case: ATP-017-A (REQ-017 validation)

**Description:** Validates REQ-017 via **Inspection** against the requirement contract.

- **User Scenario: SCN-017-A1**
    - **Given** prerequisite identity, policy, and data setup required for the requirement
    - **When** the actor executes the feature behavior under normal v1 conditions
    - **Then** the expected requirement outcome is observed with no policy or traceability violation

---

### Requirement Validation: REQ-018 (read educator subscription tier from feature 010 and apply tier-based up)

#### Test Case: ATP-018-A (REQ-018 validation)

**Description:** Validates REQ-018 via **Test** against the requirement contract.

- **User Scenario: SCN-018-A1**
    - **Given** prerequisite identity, policy, and data setup required for the requirement
    - **When** the actor executes the feature behavior under normal v1 conditions
    - **Then** the expected requirement outcome is observed with no policy or traceability violation

---

### Requirement Validation: REQ-019 (compute revenue share as 80/20 default and 85/15 for pro tier, and persi)

#### Test Case: ATP-019-A (REQ-019 validation)

**Description:** Validates REQ-019 via **Test** against the requirement contract.

- **User Scenario: SCN-019-A1**
    - **Given** prerequisite identity, policy, and data setup required for the requirement
    - **When** the actor executes the feature behavior under normal v1 conditions
    - **Then** the expected requirement outcome is observed with no policy or traceability violation

---

### Requirement Validation: REQ-020 (require JWT authentication and role checks for all learner and educator )

#### Test Case: ATP-020-A (REQ-020 validation)

**Description:** Validates REQ-020 via **Test** against the requirement contract.

- **User Scenario: SCN-020-A1**
    - **Given** prerequisite identity, policy, and data setup required for the requirement
    - **When** the actor executes the feature behavior under normal v1 conditions
    - **Then** the expected requirement outcome is observed with no policy or traceability violation

---

### Requirement Validation: REQ-021 (expose API endpoints under /api/v1 with JSON contracts aligned to platfo)

#### Test Case: ATP-021-A (REQ-021 validation)

**Description:** Validates REQ-021 via **Inspection** against the requirement contract.

- **User Scenario: SCN-021-A1**
    - **Given** prerequisite identity, policy, and data setup required for the requirement
    - **When** the actor executes the feature behavior under normal v1 conditions
    - **Then** the expected requirement outcome is observed with no policy or traceability violation

---

### Requirement Validation: REQ-022 (support compliance review and takedown workflows for copyrighted or poli)

#### Test Case: ATP-022-A (REQ-022 validation)

**Description:** Validates REQ-022 via **Test** against the requirement contract.

- **User Scenario: SCN-022-A1**
    - **Given** prerequisite identity, policy, and data setup required for the requirement
    - **When** the actor executes the feature behavior under normal v1 conditions
    - **Then** the expected requirement outcome is observed with no policy or traceability violation

---

### Requirement Validation: REQ-023 (support support-operator dispute workflows for refunds/access errors wit)

#### Test Case: ATP-023-A (REQ-023 validation)

**Description:** Validates REQ-023 via **Test** against the requirement contract.

- **User Scenario: SCN-023-A1**
    - **Given** prerequisite identity, policy, and data setup required for the requirement
    - **When** the actor executes the feature behavior under normal v1 conditions
    - **Then** the expected requirement outcome is observed with no policy or traceability violation

---

### Requirement Validation: REQ-024 (retain and back up enrollment/progress records and support controlled re)

#### Test Case: ATP-024-A (REQ-024 validation)

**Description:** Validates REQ-024 via **Analysis** against the requirement contract.

- **User Scenario: SCN-024-A1**
    - **Given** prerequisite identity, policy, and data setup required for the requirement
    - **When** the actor executes the feature behavior under normal v1 conditions
    - **Then** the expected requirement outcome is observed with no policy or traceability violation

---

### Requirement Validation: REQ-025 (enforce age-restricted content access controls based on lesson/course ma)

#### Test Case: ATP-025-A (REQ-025 validation)

**Description:** Validates REQ-025 via **Test** against the requirement contract.

- **User Scenario: SCN-025-A1**
    - **Given** prerequisite identity, policy, and data setup required for the requirement
    - **When** the actor executes the feature behavior under normal v1 conditions
    - **Then** the expected requirement outcome is observed with no policy or traceability violation

---

### Requirement Validation: REQ-026 (require knife/fire safety disclaimer acknowledgement before playback for)

#### Test Case: ATP-026-A (REQ-026 validation)

**Description:** Validates REQ-026 via **Demonstration** against the requirement contract.

- **User Scenario: SCN-026-A1**
    - **Given** prerequisite identity, policy, and data setup required for the requirement
    - **When** the actor executes the feature behavior under normal v1 conditions
    - **Then** the expected requirement outcome is observed with no policy or traceability violation

---

### Requirement Validation: REQ-027 (display refund policy terms before purchase and snapshot accepted policy)

#### Test Case: ATP-027-A (REQ-027 validation)

**Description:** Validates REQ-027 via **Test** against the requirement contract.

- **User Scenario: SCN-027-A1**
    - **Given** prerequisite identity, policy, and data setup required for the requirement
    - **When** the actor executes the feature behavior under normal v1 conditions
    - **Then** the expected requirement outcome is observed with no policy or traceability violation

---

### Requirement Validation: REQ-028 (harden playback against token replay/hotlinking through short-lived play)

#### Test Case: ATP-028-A (REQ-028 validation)

**Description:** Validates REQ-028 via **Test** against the requirement contract.

- **User Scenario: SCN-028-A1**
    - **Given** prerequisite identity, policy, and data setup required for the requirement
    - **When** the actor executes the feature behavior under normal v1 conditions
    - **Then** the expected requirement outcome is observed with no policy or traceability violation

---

## Coverage Summary

| Metric                   | Count          |
| ------------------------ | -------------- |
| Total Requirements (REQ) | 28             |
| Total Test Cases (ATP)   | 28             |
| Total Scenarios (SCN)    | 28             |
| Requirements with ≥1 ATP | 28 / 28 (100%) |
| Test Cases with ≥1 SCN   | 28 / 28 (100%) |
| **Overall Coverage**     | **100%**       |
