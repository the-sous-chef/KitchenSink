# V-Model Requirements Specification: Auth0 User Authentication

**Feature Branch**: `002-auth0-user-auth`
**Created**: 2026-05-09
**Status**: Draft
**Source**: `specs/002-auth0-user-auth/spec.md`

## Overview

Auth0-based user authentication for the Sous Chef app across web (Next.js) and mobile (Expo) platforms. Users authenticate via Auth0 using Authorization Code Flow with PKCE. Platform-specific flows apply: mobile shows the auth screen automatically; web redirects to the Auth0 login page. Sessions persist via refresh tokens. Users are identified by auto-generated UUIDv4 IDs synced to the Sous Chef database on signup via a post-registration Auth0 Action. The feature covers sign-up, login, logout, profile view, account editing, account deletion (cascading to Auth0), password reset, MFA enrollment, social account linking, user impersonation, API authorization, and user suspension/reactivation.

## Requirements

### Functional Requirements

| ID      | Description                                                                                                                                                                                               | Priority | Rationale                                                                                                                | Verification Method |
| ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------ | ------------------- |
| REQ-001 | The system SHALL authenticate users via Auth0 Authorization Code Flow with PKCE on the web platform, the mobile platform.                                                                                 | P1       | PKCE is required for public clients (mobile/SPA) to prevent authorization code interception attacks.                     | Test                |
| REQ-002 | On the mobile app, the system SHALL display the Auth0 authentication screen automatically when no valid session exists, before any app content is accessible.                                             | P1       | Mobile UX requirement — users must not see app content before authentication.                                            | Test                |
| REQ-003 | On the web app, the system SHALL redirect unauthenticated users to the Auth0 login page when they attempt to access any protected route.                                                                  | P1       | Web UX requirement — all protected routes must be gated behind authentication.                                           | Test                |
| REQ-004 | The system SHALL support Auth0 social login providers (at minimum: Google) in addition to email/password authentication.                                                                                  | P1       | Social login reduces friction for new users and is a common expectation.                                                 | Test                |
| REQ-005 | The system SHALL handle the Auth0 callback redirect URI on each platform to exchange the authorization code for OAuth tokens.                                                                             | P1       | Required to complete the OAuth 2.0 Authorization Code Flow.                                                              | Test                |
| REQ-006 | The system SHALL store mobile OAuth tokens in iOS Keychain / Android Keystore, store web session cookies with httpOnly, encrypted, SameSite=Strict attributes.                                            | P1       | Secure token storage prevents token theft via XSS (web) and app data extraction (mobile).                                | Test                |
| REQ-007 | The system SHALL silently refresh the access token using the refresh token when the access token expires, without requiring user interaction.                                                             | P1       | Session persistence — users should not be interrupted by token expiry during active use.                                 | Test                |
| REQ-008 | The system SHALL redirect the user to the Auth0 login screen when the refresh token expires / is revoked.                                                                                                 | P1       | Expired/revoked refresh tokens must force re-authentication to maintain security.                                        | Test                |
| REQ-009 | The system SHALL attach a valid access token to all API requests as a Bearer token in the Authorization header.                                                                                           | P1       | All API endpoints require authentication; the Bearer token is the standard mechanism.                                    | Test                |
| REQ-010 | The system SHALL provide a logout action that clears all local tokens (access token, refresh token) from encrypted storage.                                                                               | P1       | Logout must fully clear the local session to prevent unauthorized access on shared devices.                              | Test                |
| REQ-011 | The system SHALL revoke the refresh token in Auth0 on logout via the Auth0 revocation endpoint.                                                                                                           | P1       | Server-side revocation prevents the refresh token from being used after logout.                                          | Test                |
| REQ-012 | After logout, the system SHALL return the user to the authentication screen (mobile) / redirect to the Auth0 login page (web).                                                                            | P1       | Post-logout navigation must return the user to the unauthenticated state.                                                | Test                |
| REQ-013 | On user signup, Auth0 SHALL trigger a post-registration action (Auth0 Action) that generates a unique auto-generated user ID (UUIDv4), creates a User record in the Sous Chef database.                   | P1       | The Sous Chef database must have a User record for every Auth0 user to support all downstream features.                  | Test                |
| REQ-014 | The post-registration action SHALL also create an Account record associated with the new User.                                                                                                            | P1       | The Account entity is required for subscription tier and profile data; it must be created at signup.                     | Test                |
| REQ-015 | The auto-generated user ID SHALL be stored in the Auth0 user's `app_metadata` so it is available in all subsequent tokens, API calls.                                                                     | P1       | The canonical user ID must be propagated via tokens to avoid additional database lookups on every request.               | Test                |
| REQ-016 | The post-registration action SHALL retry database writes on transient failures (up to 3 attempts with exponential backoff).                                                                               | P1       | Transient database failures must not result in orphaned Auth0 users without Sous Chef records.                           | Test                |
| REQ-017 | A reconciliation mechanism SHALL exist to detect Auth0 users without corresponding Sous Chef database records, create the missing records.                                                                | P1       | Handles edge cases where the post-registration action fails permanently, preventing data inconsistency.                  | Test                |
| REQ-018 | The system SHALL provide a profile page that displays the authenticated user's display name, email, avatar,, account creation date sourced from the Sous Chef database.                                   | P2       | Users need to view their profile information; data is sourced from the database, not Auth0 directly.                     | Test                |
| REQ-019 | The system SHALL provide an account edit page where users can modify their display name, avatar.                                                                                                          | P2       | Users must be able to personalize their identity within the app.                                                         | Test                |
| REQ-020 | Changes to display name, avatar on the account edit page SHALL be persisted to the Sous Chef database.                                                                                                    | P2       | Account edits must be durable and reflected across all sessions.                                                         | Test                |
| REQ-021 | The account edit page SHALL display the email field as read-only with a note directing users to Auth0 for email changes.                                                                                  | P2       | Email is managed by Auth0; the app must not allow direct email editing to avoid inconsistency.                           | Inspection          |
| REQ-022 | The account edit page SHALL prevent saving when the display name is empty, SHALL display a validation error.                                                                                              | P2       | Display name is a required field; empty values must be rejected client-side.                                             | Test                |
| REQ-023 | The system SHALL provide an account deletion flow that requires explicit confirmation (e.g., typing "DELETE") before proceeding.                                                                          | P2       | Account deletion is irreversible; explicit confirmation prevents accidental deletion.                                    | Test                |
| REQ-024 | On confirmed account deletion, the system SHALL remove the User records plus Account records from the Sous Chef database, then remove the user from Auth0.                                                | P2       | GDPR and trust requirement — all user data must be removed from both systems on deletion.                                | Test                |
| REQ-025 | Account deletion SHALL cascade to all user-owned data (recipes, meal plans, etc.) in the Sous Chef database.                                                                                              | P2       | Orphaned user-owned data must not remain after account deletion.                                                         | Test                |
| REQ-026 | After account deletion, the system SHALL log the user out, return them to the authentication screen.                                                                                                      | P2       | Post-deletion the session is invalid; the user must be returned to the unauthenticated state.                            | Test                |
| REQ-027 | The system SHALL provide access to Auth0's password reset flow from the login screen via a "Forgot Password" link.                                                                                        | P2       | Users must be able to recover access to their account if they forget their password.                                     | Test                |
| REQ-028 | The password reset flow SHALL be handled entirely by Auth0 — the Sous Chef backend SHALL NOT store, process, / have access to user passwords.                                                             | P1       | Security requirement — passwords must never pass through or be stored by the Sous Chef backend.                          | Inspection          |
| REQ-029 | The system SHALL provide access to Auth0's MFA enrollment flow from the account settings page.                                                                                                            | P3       | MFA is a security enhancement for users who opt in; access must be surfaced from account settings.                       | Test                |
| REQ-030 | The system SHALL support at minimum TOTP (authenticator app) as an MFA method via Auth0.                                                                                                                  | P3       | TOTP is the most widely supported and secure MFA method for consumer apps.                                               | Test                |
| REQ-031 | When MFA is enrolled, Auth0 SHALL require the second factor on all subsequent login attempts.                                                                                                             | P3       | MFA enrollment must be enforced by Auth0 on every login to provide the security benefit.                                 | Test                |
| REQ-032 | The system SHALL allow a logged-in user to link additional social provider accounts (e.g., Google) to their existing Auth0 identity from the account settings page via the Auth0 Management API.          | P2       | Users may want to add social login options to their existing account.                                                    | Test                |
| REQ-033 | The system SHALL allow a logged-in user to unlink a social provider account from their Auth0 identity, provided at least one login method remains active.                                                 | P2       | Users must be able to remove social providers, but must retain at least one login method.                                | Test                |
| REQ-034 | When a user links / unlinks a social provider, the system SHALL NOT create a new User / Account record in the Sous Chef database — the existing canonical user ID SHALL be preserved.                     | P2       | Social linking/unlinking is an Auth0-level operation; the Sous Chef user identity must remain stable.                    | Test                |
| REQ-035 | The system SHALL support user impersonation for authorized support/engineering personnel via Auth0's impersonation / token exchange mechanism.                                                            | P2       | Support and engineering personnel need to impersonate users to diagnose issues.                                          | Test                |
| REQ-036 | Impersonation sessions SHALL be clearly distinguishable from normal sessions — all API requests made during impersonation SHALL include an impersonation flag, the impersonator's identity in audit logs. | P2       | Audit trail requirement — impersonation actions must be attributable to the impersonator.                                | Test                |
| REQ-037 | Impersonation SHALL NOT allow password changes, account deletion, / MFA modifications on behalf of the impersonated user.                                                                                 | P2       | Impersonation must be scoped to read/support operations; destructive actions are prohibited.                             | Test                |
| REQ-038 | All Sous Chef API endpoints SHALL require a valid Auth0 access token. Requests without a valid token SHALL receive `401 Unauthorized`.                                                                    | P1       | All API endpoints must be protected; unauthenticated requests must be rejected.                                          | Test                |
| REQ-039 | The API Gateway authorizer SHALL validate Auth0 JWT tokens (signature, expiration, audience, issuer) on every request.                                                                                    | P1       | JWT validation is the security gate for all API access; all claims must be verified.                                     | Test                |
| REQ-040 | The access token SHALL include the Sous Chef user ID (from `app_metadata`) as a custom claim, enabling the API to identify the requesting user without an additional database lookup.                     | P1       | Performance and simplicity — the user ID in the token avoids a database round-trip on every request.                     | Test                |
| REQ-041 | The system SHALL support suspending a user by both blocking the user in Auth0 (via the Management API "block" flag), setting the User entity `status` to `suspended` in the Sous Chef database.           | P2       | Suspension must be enforced at both the Auth0 level (blocks new tokens) and the database level (blocks existing tokens). | Test                |
| REQ-042 | The API Gateway authorizer SHALL deny access (return `403 Forbidden`) to users whose Auth0 account is blocked, even if the access token is otherwise valid.                                               | P2       | Suspended users must be denied access even with a currently valid token.                                                 | Test                |
| REQ-043 | A suspended user who attempts to log in SHALL be shown a clear message indicating their account has been suspended.                                                                                       | P2       | Users must receive actionable feedback when their account is suspended.                                                  | Test                |
| REQ-044 | The system SHALL support reactivating a suspended user by unblocking them in Auth0, setting the User entity `status` back to `active`.                                                                    | P2       | Suspension must be reversible to support account reinstatement.                                                          | Test                |

### Non-Functional Requirements

| ID         | Description                                                                                                                                                                                                                                                                                                                      | Priority | Rationale                                                                                                         | Verification Method |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ----------------------------------------------------------------------------------------------------------------- | ------------------- |
| REQ-NF-001 | All TypeScript code in auth-related workspaces SHALL compile with `strict: true`; no `any` used outside explicitly marked test doubles. Auth0 SDK types, token interfaces,, API response types SHALL use strict typing with `interface` for data shapes, `type` for unions/aliases.                                              | P1       | Type safety prevents runtime errors in auth flows where failures have security implications.                      | Inspection          |
| REQ-NF-002 | All exported functions, classes, interfaces, type aliases,, interface fields in auth-related code SHALL carry JSDoc block comments. Auth middleware, token refresh handlers,, post-registration actions SHALL include `@param`, `@returns`,, `@throws` tags.                                                                     | P2       | Documentation enables maintainability and onboarding for security-critical auth code.                             | Inspection          |
| REQ-NF-003 | All imports within auth-related workspaces SHALL use aliased paths (`@shared/*`, `@web/*`, `@armoury/<pkg>`) with `.js`/`.jsx` extensions. No `helpers/` directories.                                                                                                                                                            | P2       | Consistent import paths enforce workspace boundaries and prevent circular dependencies.                           | Inspection          |
| REQ-NF-004 | All auth-related UI components (login screens, profile page, account edit page, deletion confirmation dialog) SHALL expose accessible names queryable via `getByRole`/`getByLabel` in Playwright tests. `data-testid` is prohibited.                                                                                             | P1       | Accessibility requirement — auth UI must be usable by screen reader users and testable without brittle selectors. | Test                |
| REQ-NF-005 | Auth status indicators (logged in, logged out, session expired, MFA required) SHALL NOT rely on color alone. Each status SHALL be paired with a text label / icon.                                                                                                                                                               | P1       | WCAG 2.1 compliance — color-blind users must be able to distinguish auth states.                                  | Inspection          |
| REQ-NF-006 | Any new auth-related workspace SHALL be registered in the root `package.json` workspaces array, SHALL extend `@armoury/typescript`, `@armoury/eslint`, `@armoury/prettier`,, `@armoury/vitest` shared configs. Turbo task dependencies SHALL be declared.                                                                        | P1       | Monorepo consistency — all workspaces must conform to shared tooling configuration.                               | Inspection          |
| REQ-NF-007 | All auth-related code SHALL pass `turbo run typecheck`, `turbo run lint`,, `turbo run format:check` with zero errors before merge.                                                                                                                                                                                               | P1       | CI gate — code quality checks must pass before any auth code is merged.                                           | Test                |
| REQ-NF-008 | Tests SHALL conform to the testing pyramid: >= 70% unit, <= 20% integration, <= 10% E2E. Each test file SHALL open with a block comment mapping requirement IDs (FR-xxx) to test case descriptions.                                                                                                                              | P1       | Testing discipline — the pyramid ensures fast feedback; requirement mapping enables traceability.                 | Inspection          |
| REQ-NF-009 | Custom errors (e.g., `AuthSessionExpiredError`, `UserNotFoundError`, `AccountDeletionFailedError`) SHALL extend `Error`, SHALL expose a type guard (`isXxxError(e: unknown): e is XxxError`).                                                                                                                                    | P1       | Structured error handling enables precise error recovery and prevents swallowed errors in auth flows.             | Inspection          |
| REQ-NF-010 | All date fields in auth-related interfaces (e.g., `createdAt`, `lastLoginAt`, `tokenExpiresAt`) SHALL be ISO 8601 strings, never `Date` objects.                                                                                                                                                                                 | P1       | Serialization consistency — ISO 8601 strings are safe across JSON boundaries and timezone-agnostic.               | Inspection          |
| REQ-NF-011 | The login/signup UI SHALL consume design system tokens (`--accent-primary`, `--accent-secondary`, semantic status colors) from the shared token set. Hard-coded color values in component styles are prohibited.                                                                                                                 | P2       | Design consistency — auth UI must use the shared design system to maintain visual coherence.                      | Inspection          |
| REQ-NF-012 | All auth-related backend functions (login callback, token refresh, post-registration action, account deletion, reconciliation job) SHALL emit structured logs in JSON format with correlation IDs, ISO 8601 timestamps. CloudWatch Logs SHALL be configured as the log sink.                                                     | P1       | Observability — structured logs enable log aggregation, search, and alerting in CloudWatch.                       | Test                |
| REQ-NF-013 | CloudWatch Logs SHALL drain to Sentry for centralized error tracking, alerting. All unhandled exceptions, explicit error events in auth flows SHALL surface as Sentry issues with full stack traces, request context.                                                                                                            | P1       | Centralized error tracking enables rapid incident response for auth failures.                                     | Test                |
| REQ-NF-014 | Auth flows SHALL emit CloudWatch custom metrics: login success/failure count, token refresh success/failure count, signup success/failure count, account deletion success/failure count, reconciliation job runs/repairs/failures. Metrics SHALL use standard CloudWatch namespaces, dimensions (per-environment, per-platform). | P1       | Metrics enable monitoring dashboards and alerting on auth flow health.                                            | Test                |
| REQ-NF-015 | Client-side auth errors (failed login attempts, token refresh failures, session expiry events) SHALL be captured by the Sentry client SDK with breadcrumbs for the preceding auth state transitions.                                                                                                                             | P2       | Client-side error visibility enables diagnosis of auth failures that never reach the backend.                     | Test                |
| REQ-NF-016 | Distributed tracing SHALL be enabled across auth flows — from client request through API Gateway authorizer to backend handlers. Trace IDs SHALL propagate via standard AWS X-Ray / OpenTelemetry headers.                                                                                                                       | P2       | End-to-end tracing enables diagnosis of latency and failure across the distributed auth stack.                    | Test                |
| REQ-NF-017 | The observability architecture SHALL allow future integration of LogRocket (session replay), NewRelic (or equivalent APM) without requiring changes to application logging / metrics instrumentation.                                                                                                                            | P3       | Future-proofing — the observability stack must not be tightly coupled to specific vendors.                        | Analysis            |

### Interface Requirements

| ID         | Description                                                                                                                                                                                   | Priority | Rationale                                                                                       | Verification Method |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ----------------------------------------------------------------------------------------------- | ------------------- |
| REQ-IF-001 | The system SHALL integrate with Auth0 using the `@auth0/nextjs-auth0` v4.x SDK for the web (Next.js) platform.                                                                                | P1       | The Auth0 Next.js SDK provides the standard integration for Auth0 with Next.js App Router.      | Inspection          |
| REQ-IF-002 | The system SHALL integrate with Auth0 using the `react-native-auth0` v5.5 SDK for the mobile (Expo) platform.                                                                                 | P1       | The react-native-auth0 SDK provides the standard integration for Auth0 with Expo/React Native.  | Inspection          |
| REQ-IF-003 | The mobile platform SHALL use `expo-encrypted-store` for encrypted token storage (Keychain on iOS, Keystore on Android).                                                                      | P1       | expo-secure-store is the standard Expo mechanism for OS-level secure storage.                   | Inspection          |
| REQ-IF-004 | The API Gateway authorizer SHALL use `jwks-rsa`, `jose` libraries to validate Auth0 JWT tokens.                                                                                               | P1       | These libraries provide standards-compliant JWT validation with JWKS key fetching.              | Inspection          |
| REQ-IF-005 | The post-registration Auth0 Action, backend handlers SHALL use `@aws-sdk/client-sqs` for SQS integration (version-archive queue).                                                             | P1       | SQS is the message queue for async operations in the auth flow.                                 | Inspection          |
| REQ-IF-006 | Backend auth functions SHALL use `@sentry/aws-serverless` for Sentry integration, `@aws-lambda-powertools/logger` for structured logging.                                                     | P1       | These are the standard observability libraries for AWS Lambda functions in this project.        | Inspection          |
| REQ-IF-007 | Infrastructure SHALL be defined using AWS CDK v2 (`aws-cdk-lib`).                                                                                                                             | P1       | CDK v2 is the infrastructure-as-code standard for this project.                                 | Inspection          |
| REQ-IF-008 | The Auth0 post-registration action SHALL call the Sous Chef backend API (or directly write to the database) to create User, Account records, passing the Auth0 `sub` claim, generated UUIDv4. | P1       | The post-registration action is the integration point between Auth0 and the Sous Chef database. | Test                |
| REQ-IF-009 | The API Gateway authorizer SHALL return a standard IAM policy document allowing / denying access based on JWT validation, user suspension status.                                             | P1       | API Gateway requires a standard IAM policy response from Lambda authorizers.                    | Test                |
| REQ-IF-010 | The system SHALL expose a reconciliation API endpoint (or scheduled job) that accepts Auth0 user list data, creates missing Sous Chef database records.                                       | P1       | The reconciliation mechanism requires an interface to receive Auth0 user data.                  | Test                |

### Constraint Requirements

| ID         | Description                                                                                                                                                                                                                                                                           | Priority | Rationale                                                                                                            | Verification Method |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------- | ------------------- |
| REQ-CN-001 | The system SHALL run on Node.js 22.x for Lambda runtime functions.                                                                                                                                                                                                                    | P1       | Node.js 22.x is the Lambda runtime specified in the project tech stack.                                              | Inspection          |
| REQ-CN-002 | The Sous Chef backend SHALL NOT store, process, / have access to user passwords at any point. All password management SHALL be delegated entirely to Auth0.                                                                                                                           | P1       | Security constraint — passwords must never leave Auth0's control.                                                    | Inspection          |
| REQ-CN-003 | The canonical user identifier across all Sous Chef systems SHALL be the auto-generated UUIDv4 created at signup. The Auth0 `sub` claim (`auth0Id`) SHALL be used only for Auth0 API calls, SHALL NOT be used as the primary identifier in the Sous Chef database / application logic. | P1       | Decoupling from Auth0's internal identifiers prevents vendor lock-in and enables future identity provider migration. | Inspection          |
| REQ-CN-004 | Account deletion SHALL be permanent, irreversible. The system SHALL NOT implement soft-delete for User / Account records.                                                                                                                                                             | P2       | GDPR compliance — deleted user data must be fully removed, not merely hidden.                                        | Inspection          |
| REQ-CN-005 | The email change flow is explicitly out of scope for this feature. Users SHALL NOT be able to change their email address through the Sous Chef app in this feature.                                                                                                                   | P2       | Email changes require a separate feature coordinating with Auth0's email verification flow.                          | Inspection          |
| REQ-CN-006 | Admin UI for user management (viewing, searching, editing, bulk-managing users) is explicitly out of scope. Suspension/reactivation, impersonation SHALL be backend/API operations only.                                                                                              | P2       | Admin UI is a separate feature; this feature delivers only the backend operations.                                   | Inspection          |
| REQ-CN-007 | The system SHALL deploy to AWS using CDK v2. No other infrastructure-as-code tools (Terraform, SAM, CloudFormation templates) SHALL be used for resources defined in this feature.                                                                                                    | P1       | CDK v2 is the mandated IaC tool for this project.                                                                    | Inspection          |
| REQ-CN-008 | All auth-related workspaces SHALL extend the shared `@armoury/typescript`, `@armoury/eslint`, `@armoury/prettier`,, `@armoury/vitest` configurations. Deviations from shared configs are prohibited without explicit constitution amendment.                                          | P1       | Monorepo consistency constraint — all workspaces must conform to shared tooling.                                     | Inspection          |

## Assumptions

- Auth0 tenant is already provisioned and configured with the appropriate application registrations (web and mobile) before implementation begins.
- The Sous Chef database (RDS PostgreSQL 16) is available and the schema for `User` and `Account` entities is defined (or will be defined as part of this feature).
- Auth0 Actions are available in the Auth0 tenant plan.
- The `@armoury` shared configuration packages (`@armoury/typescript`, `@armoury/eslint`, `@armoury/prettier`, `@armoury/vitest`) are published and available in the monorepo.
- Social login with Google is the minimum required provider; additional providers may be added without requiring a spec change.
- The Auth0 Management API credentials required for account deletion, social linking/unlinking, suspension, and impersonation will be securely stored in AWS Secrets Manager.
- The reconciliation mechanism will run as a scheduled Lambda function (e.g., EventBridge Scheduler) rather than a real-time process.

## Dependencies

- **Auth0 tenant** — provisioned with web and mobile application registrations, post-registration Actions enabled, and Management API access.
- **RDS PostgreSQL 16** — the Sous Chef database must be accessible from Lambda functions in the same VPC.
- **AWS API Gateway** — the Lambda authorizer is attached to the API Gateway that fronts all Sous Chef API endpoints.
- **AWS SQS** — required for the version-archive queue referenced in the tech stack.
- **AWS CDK v2** — infrastructure definition and deployment.
- **`@armoury` shared configs** — monorepo shared tooling packages must be available.
- **001-sous-chef-recipe-app** — downstream consumer of the authentication layer (FR-045).
- **003-usda-food-data** — downstream consumer of the shared API Gateway authorizer (FR-035).
- **005-ai-integration** — downstream consumer of the auth layer for external agent OAuth (FR-018).
- **010-subscriptions** — downstream consumer of the Account entity's `subscriptionTier` field.

## Glossary

| Term                              | Definition                                                                                                                     |
| --------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| Auth0                             | The third-party identity provider used for authentication and authorization.                                                   |
| Authorization Code Flow with PKCE | OAuth 2.0 flow for public clients (mobile/SPA) that uses a code verifier/challenge to prevent authorization code interception. |
| Access Token                      | A short-lived JWT issued by Auth0 that grants access to Sous Chef API endpoints.                                               |
| Refresh Token                     | A long-lived token used to obtain new access tokens without user re-authentication.                                            |
| `app_metadata`                    | Auth0 user metadata that is not modifiable by the user; used to store the canonical Sous Chef user ID.                         |
| UUIDv4                            | Universally Unique Identifier version 4 (random); the canonical user identifier in Sous Chef.                                  |
| Post-Registration Action          | An Auth0 Action that fires after a user successfully registers, used to sync the user to the Sous Chef database.               |
| API Gateway Authorizer            | An AWS Lambda function that validates Auth0 JWTs and returns an IAM policy to API Gateway.                                     |
| Reconciliation                    | The process of detecting and repairing Auth0 users that lack corresponding Sous Chef database records.                         |
| Impersonation                     | The ability for authorized personnel to act as a specific user for support/debugging purposes.                                 |
| Suspension                        | The state where a user is blocked in Auth0 and denied API access; reversible by reactivation.                                  |
| TOTP                              | Time-based One-Time Password; the primary MFA method supported via Auth0.                                                      |
| PKCE                              | Proof Key for Code Exchange; a security extension to OAuth 2.0 for public clients.                                             |
| JWKS                              | JSON Web Key Set; the Auth0 endpoint that publishes public keys for JWT signature verification.                                |
| Canonical User ID                 | The auto-generated UUIDv4 that is the primary identifier for a user across all Sous Chef systems.                              |

---

**Total Requirements**: 79
**By Category**: Functional: 44 | Non-Functional: 17 | Interface: 10 | Constraint: 8
**By Priority**: P1: 52 | P2: 24 | P3: 3
**By Verification Method**: Test: 46 | Inspection: 30 | Analysis: 1 | Demonstration: 0
